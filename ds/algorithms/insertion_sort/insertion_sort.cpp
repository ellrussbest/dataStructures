#include <iostream>

// insertion sort
void insertion_sort(int* arr, int size) {
    int i, j, key;

    for(j = 1; j < size; j++) {
        key = arr[j];
        i = j - 1;

        while (i >= 0 && arr[i] > key)
        {
            arr[i+1] = arr[]
        }
        
    }
}

int main () {
    std::cout << "Hello world \n";
}